
**To Do** :

- index.html basic divs and choose to show different types of fractals 
- in src/fractals make 2-3 default fractals like Sierpinski Triangle , snow flake pattern and all recursion/maths there
- in src/rendering make some rendering logic ( this is not some image based )
- write src/main.js for all DOM handler and rendering 
- use src/utils for things out of above directory 

!